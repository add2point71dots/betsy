require "test_helper"

describe Product do
  let(:product) { Product.new }

  describe "validation" do
    it "must be valid" do
      value(product).must_be :valid?
    end

    it "must be valid" do
      value(product).must_be :valid?
    end

    it "must be valid" do
      value(product).must_be :valid?
    end

    it "must be valid" do
      value(product).must_be :valid?
    end

    

  end
end
