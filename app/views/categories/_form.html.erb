<%= form_for @category do |f| %>

     <ul>
          <% @category.errors.each do |field, msg| %>
               <li><%= msg %></li>
          <% end %>
     </ul>

     <%= f.label :label %>
     <%= f.text_field :label %>

     <%= f.submit %>

<% end %>
